# Web + API
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
jinja2>=3.1
python-multipart>=0.0.12
itsdangerous>=2.2

# Data + settings
sqlmodel>=0.0.22
pydantic-settings>=2.4
apscheduler>=3.10,<4.0
httpx>=0.27

# Security
cryptography>=43.0
bcrypt>=4.2
tzdata>=2024.1  # IANA tz database for zoneinfo (date/"this week" logic)

# RAG: vector store, embeddings, document parsing
chromadb>=0.5.0
fastembed>=0.4.0
pymupdf>=1.24
python-docx>=1.1
openpyxl>=3.1

# PDF generation (printable BigCommerce order PDFs)
reportlab>=4.0
